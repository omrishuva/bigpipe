<div class="row pull-left">
  <h5 ><%="#{params[:type].pluralize.capitalize}"%></h5>
  <span class="lead" ><%="Total: #{@users.size}"%></span>
  <span>|</span>
  <span class="lead"><%="Today: #{@today}"%></span>
  <span>|</span>
  <span class="lead"><%="Yesterday: #{@yesterday}"%></span>
</div>
</br>
</br>
</br>

<a href="/trainer/new">Add</a>

</br></br>
<table class="table table-hover pull-left">
  <tr>
    <th>Name</th>
    <th>Joined at</th>
  </tr>
 
  <% @users.each do |user| %>
    <tr>
      <td><%= user.name %></td>
      <td><%= user.created_at.strftime("%B %d, %Y") %></td>
    </tr>
  <% end %>
</table>
