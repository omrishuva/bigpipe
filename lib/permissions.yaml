controllers:
  users:
    users: admin
    add_service_provider: admin
    profile: consumer
  activities:
    new_activity: owner_or_admin

views:
  user_navtabs:
    permissions:
      about_me: ["admin", "service_provider"]
      my_activities: [ "trainer", "tour_guide" ]
      my_locations: ["location_owner"]
      my_schedule: ["consumer", "service_provider"]
      goals: ["consumer"]
      stats: ["service_provider"]
    default_active:
      consumer: "my_schedule"
      trainer: "my_activities"
      location_owner: "stats"
      admin: "my_schedule"
